# Changelog

## Unreleased

### Phase 0

- 初始化 `rl_admittance_rehab_ws` 项目结构。
- 添加完整项目规范副本 `PROJECT_SPEC.md`。
- 添加 YAML 配置模板、配置加载器、日志配置和基础测试。
- 暂未实现任何 MuJoCo、导纳控制、患者模型、RL、页面、Agent 或 ROS2 业务逻辑。

### Phase 1

- 纳入三自由度 MuJoCo 模型和 CAD mesh 资产。
- 添加平面 3R 正向运动学和解析 Jacobian。
- 添加末端 `tool_tip` site 与 `[Fx, Fy, Tz]` 外力注入接口。
- 添加关节目标、关节限位、工作空间检查和数值稳定性检测。
- 添加 Phase 1 可视化/无界面运行脚本及集成测试。

### Phase 2

- 添加固定参数三自由度任务空间导纳控制器。
- 添加力信号低通滤波、软死区、速度/加速度限幅和工作空间裁剪。
- 添加仿真侧阻尼最小二乘 IK 目标映射。
- 添加阶跃、正弦和反向力基线实验及 CSV/SVG/JSON 输出。
- 添加固定控制器稳定性、方向性、限幅和参数响应测试。

### Phase 3

- 添加轻度、中度、重度参数化虚拟患者。
- 实现基础阻抗力、反应延迟、方向偏置、噪声和震颤。
- 实现基于患者主动功率的疲劳累积与休息恢复。
- 添加随机种子可重复性和不同能力等级差异测试。
- 添加患者力、疲劳和主动功率 CSV/SVG 演示输出。

### Phase 4

- 添加点到点、圆轨迹和八字轨迹三个 Gymnasium 连续控制环境。
- 定义低频导纳参数增量动作空间和结构化观测空间。
- 添加跟踪进度、交互力、运动突变、辅助能量、患者主动功率和参数变化奖励分项。
- 添加任务成功、有限时域和仿真异常终止逻辑。
- 添加 SB3 `check_env`、随机策略无 NaN 和固定种子可重复性集成测试。
- 添加 `scripts.check_phase4_envs` 环境验证入口；暂不实现 SAC 训练和安全部署。

### Phase 5

- 添加基于 Stable-Baselines3 `SAC("MultiInputPolicy")` 的参数调节训练脚本。
- 使用 `VecNormalize` 对结构化观测和奖励进行训练归一化。
- 添加 TensorBoard 日志、定期 checkpoint、replay buffer 和 VecNormalize 统计保存。
- 添加自动确定性评估、成功率历史和多随机种子汇总。
- 添加模型/归一化统计的保存加载验证，以及配置哈希和 Git commit 元数据。
- 添加奖励、交互力、参数变化量和参数振荡率评估指标；未实现 Phase 6 独立安全部署层。

### Phase 6

- 添加独立于 RL/SB3 的安全参数投影器和参数变化率限制。
- 添加参数边界、交互力/力矩、任务速度/加速度和策略推理超时检查。
- 添加模型加载失败、推理异常、NaN、传感器掉线和力超限的保守参数回退。
- 添加模型无关的确定性安全策略运行时接口。
- 添加边界、变化率、NaN、模型加载失败、超时和交互力故障单元测试。
- 明确所有 Phase 6 阈值为仿真占位，暂不接入真实机器人、页面、Agent 或 ROS2。

### Phase 7

- 添加 FastAPI 仿真训练会话状态机和 REST 控制接口。
- 添加 20 Hz WebSocket 遥测流，覆盖任务状态、轨迹、交互力、导纳参数和安全状态。
- 添加 React/Vite 训练控制台、实时轨迹/力/参数曲线和训练报告视图。
- 支持开始、暂停、继续、停止、固定导纳/RL 模式切换和训练完成摘要。
- 添加后端 REST/WebSocket 集成测试和前端 TypeScript/Vite 构建验证。
- 当前页面数据源为 simulation-only，未实现 Phase 8 交互 Agent 或 ROS2。

### Phase 8

- 添加独立的规则式交互 Agent、阈值配置和结构化事件模型。
- 添加跟踪误差、交互力、速度、患者不活跃、疲劳和安全状态事件检测。
- 添加事件冷却、结构化日志、可选语音回调和训练总结模板。
- 将 Agent 事件接入 FastAPI 快照、20 Hz WebSocket 和 `GET /api/agent/events` 审计接口。
- 添加前端实时反馈提示和训练总结卡片。
- 验证 Agent 异常不会影响仿真控制/安全层；本阶段不使用 LLM API，也不接入 ROS2。

### Phase 9

- 添加 `ros2_ws/src/rehab_interfaces` 自定义消息和任务控制服务。
- 添加仿真/ROS2 驱动共用的 `RobotAdapter`、ROS2 状态桥和任务空间参数命令边界。
- 添加固定参数/确定性参数策略节点，策略输出经过独立安全监督器，不发布电机力矩或电流。
- 添加独立通信看门狗、通信中断回退和低速测试模式。
- 添加 ROS2 任务管理节点、配置文件、核心单元测试、colcon 构建验证和 topic smoke test。
- 默认硬件驱动保持关闭，真实机器人阈值和台架/人体测试仍未完成；不进入 Phase 10 实验包装。
