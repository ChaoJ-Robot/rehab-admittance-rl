schema_version: 1

safety:
  enabled: false
  # These thresholds are placeholders until the robot and sensor data sheets
  # are provided. No real robot operation is permitted with this template.
  interaction_force_limit: TODO_CONFIGURE
  interaction_torque_limit: TODO_CONFIGURE
  task_speed_limit: TODO_CONFIGURE
  task_acceleration_limit: TODO_CONFIGURE
  workspace: TODO_CONFIGURE
  parameter_bounds: TODO_CONFIGURE
  parameter_rate_limits: TODO_CONFIGURE
  policy_timeout_ms: TODO_CONFIGURE
  fallback_parameters: TODO_CONFIGURE

# Simulation termination thresholds used by Phase 4. These are not hardware
# safety limits and must be replaced before real-robot work.
simulation_limits:
  interaction_force_norm: 5.0
  task_speed_norm: 0.50
  task_acceleration_norm: 3.0
  stagnation_seconds: 10.0
  source: simulation_placeholder
  hardware_validation_required: true
