# DESIGN

<!-- impeccable:design-schema 1 -->

## Visual World

蓝白医疗管理台（Beihang Blue & White Medical Workbench），2026-08-09 用户 brief 明确指定（替代此前机场标识黄方向）。产品定位：面向医生/管理员的康复训练管理控制台（患者档案数据库 + 训练方案配置 + 实时监控 + 智能辅助）；患者交互页面为独立系统，本端不面向患者。

- THESIS：面向医生的康复训练管理控制台——患者档案、训练方案、实时监控与智能辅助在一个蓝白工作台上各就其位；拒绝深色发光与大面积高饱和配色。
- STORY：医生打开工作台看到患者档案与任务方案，为患者配置训练参数，按下蓝色开始按钮进入实时监控，巨型指标一眼扫读，智能教练随时待命。

## Palette

| 角色 | 值 | 用途 |
|---|---|---|
| 北航蓝 `--buaa-blue` | `#004a8f`（深 `#003a70`、中 `#1e5fa8`） | 顶栏带、主按钮、选中态、链接、用户气泡、滑块 |
| 浅蓝层次 | `--blue-soft` `#e8f1fa` / `--blue-softer` `#f4f8fd` / `--blue-line` `#c9dcf0` | 图标块、选中底、边框、摘要底 |
| 地面 `--ground` | `#f2f6fb`（带两团极淡蓝光晕） | 页面底色 |
| 面板 `--surface` | `#ffffff` | 卡片、输入、图表底 |
| 墨 `--ink` | `#182030`（软 `#33405a`） | 正文 |
| 次级 `--text-dim` | `#55647a` / `--text-faint` `#8494a8` | 辅助文字 |
| 状态 | 绿 `#1e9e63` 安全 / 琥珀 `#e08e0b` 警告 / 红 `#d64545` 停止 | 安全芯片、阶段指示、危险 |
| 级别徽章 | 绿 `#157a4c` / 琥珀 `#8f5400` / 红 `#b53434`（均 ≥4.5:1） | 任务难度 |
| 图表 | 参考灰蓝 `#a4b4c8` 虚线 / 实际蓝 `#00509e` | 轨迹与曲线 |
| 指标图标 | 进度青 `#0e8fb5` / 得分深蓝 `#00509e` / 力琥珀 `#e08e0b` / 疲劳绿 `#1e9e63` | 指标卡 |

色彩策略：Restrained（蓝白中性 + 语义状态色）；蓝白主色调为用户明确 brief（2026-08-09），不采用黄/橙大面积配色。

## Type

- 字体栈：`system-ui, "PingFang SC", "Microsoft YaHei", sans-serif`（规避过度使用字体）。
- 层级：顶栏 h1 19px/700；hero 22–30px/800；任务卡标题 16px/800；面板标题 14.5px/800；指标数字 26px/800 等宽数字；正文 12–13px；辅助 9–10.5px。
- 规则：标题不加 kicker/eyebrow 标签；无渐变文字。

## Components

- 顶栏：北航蓝渐变带（`#00539c→#004a8f→#003a70`）+ 底部阴影；白色北航 logo（`public/buaa-logo.png`，官网下载版）；右侧连接状态 chip 与 SIMULATION 标签（半透明白）。
- 任务卡：白底 + 浅蓝边框 + 浅蓝图标块（蓝线 SVG）；hover 蓝边加深 + 上浮；选中态整卡蓝色渐变 + 白字。
- 主按钮：蓝色渐变白字、深蓝描边；hover 提亮；active 下压。次级按钮白底蓝边；危险按钮红系。
- 患者档案卡：白卡 + 蓝色标题 + 虚线分隔（登机牌式"标签+值"两行项）。
- 参数面板：白卡 + 标题与说明 + 细线分隔；滑块蓝色圆钮（白心蓝圈）。
- 指标卡：白卡 + 左上彩色图标块 + 26px 大数字 + 底部细进度条（scaleX 动画）。
- 图表：浅底 `#fbfdff`、浅灰蓝网格、参考灰蓝虚线、实际轨迹深蓝实线、迷宫墙红。
- 聊天：白面板；AI 气泡浅蓝白底、用户气泡蓝底白字、LLM 生成浅绿底；头像蓝（用户侧中蓝）。
- 状态反馈条：按 severity 着色（信息蓝、正向绿、警告琥珀、严重红），图标色块承载语义。

## Topology

- 首页工作台：顶栏 → hero（左标题文案 + 右 4 数据概览块：在档患者/累计训练/最近训练/遥测状态）→ 双栏 workbench（左栏 368px sticky：患者档案卡[输入/最近患者列表/档案摘要/历史训练记录表] + 训练设置卡[配置/模式/当前任务/开始按钮]；右栏：任务网格 auto-fill minmax(238px) + 参数卡）→ 系统状态页脚。
- 训练页双栏：主栏（状态行→指标行→图表区[主图+力曲线+导纳参数]→总结横幅）+ 侧栏聊天（智能教练，辅助医生）。
- 响应式：≤1100px 训练页单栏；≤720px 任务网格单列、控制栏纵向、home-layout 整体滚动（task-grid 不参与 flex 压缩）。

## Motion

- 克制动效：任务卡 hover 上浮 + 阴影过渡；选中态渐变过渡；指标进度条 scaleX 过渡；无堆叠入场动画。

## Rules

- 蓝白主色调（用户 brief）；明亮环境浅色主题。
- 禁用：渐变文字、玻璃拟态、kicker/eyebrow、侧边色条（side-tab）、圆角卡厚顶边、Inter/Arial 系字体、大面积黄/橙。
- 北航品牌：logo 仅使用官方白色版于深蓝顶栏；北航蓝为唯一品牌强调色。
- 医生视角文案：界面面向医生/管理员，不出现面向患者的交互引导。

## Sidecar

- 视觉方向：user brief override（2026-08-09），替代机场黄方向（原 seed 936d481e 已废弃）。
- 检测状态：`detect.mjs` 于 2026-08-09 对 frontend/src 全量 CLEAN。
- 验收：桌面 1400×900 首页/训练页 DOM 计算样式审计通过；移动 394×848 无溢出、可滚动、卡片堆叠正常。
- 排版修复（2026-08-09）：任务卡由横向错行改为纵向三行布局（图标+标题+选中 / 分类+难度 / 描述）；≤1100px 窄屏训练页改为单列自适应（面板固定高度、页面整体滚动），修复图表塌陷与聊天面板重叠。复验：871px 单列模式下三图表全部可见、无重叠、全部内容可达（截图 /home/jack/Desktop/RL/ROBOT/ui_fixed_training.png）。
- 工作台重构（2026-08-09，第二轮）：首页由通栏单列改为双栏工作台，消除宽屏留白；新增 hero 数据概览（/api/patients 实时数据：在档患者/累计训练/最近训练/遥测状态）、系统状态页脚。验证：浏览器功能审计通过，detect.mjs CLEAN。
- 临床功能扩充（2026-08-09，第三轮）：新增患者总览看板（每卡：训练次数/近3次均分/最近训练 + 得分趋势 Sparkline + ▲好转/▼下滑/—持平判定 + 打开档案）；档案摘要增“近 3 次平均得分”临床指标；训练页急停按钮独立醒目化；hero 概览改 auto-fit。实测验证通过，detect.mjs CLEAN。
- Agent 临床决策支持改造（2026-08-09，第四轮）：Agent 由“鼓励机器人”升级为医生端临床决策助手。新增 rehab_sim/agent/clinical.py（确定性、无 LLM 依赖）：assess_history 纵向趋势评估（近 8 次最小二乘斜率 → 改善/平台期/下滑/数据不足 + 临床 flag）；recommend_next_session 渐进负荷处方（完成率≥80% 升级难度梯度、<45% 降级、顶级任务任务内加量；稳定改善者建议 RL 模式，附中文依据）。新 API：GET /api/agent/assessment/{id}、/api/agent/prescription/{id}；chat 上下文注入患者历史与趋势结论。前端左栏新增 Agent 临床评估卡（趋势徽章+指标+flag+处方建议+“采纳并填入训练设置”一键闭环，human-in-the-loop）。验证：102 测试全过（新增 13），真实数据 P100 平台期 92% 完成率 → 迷宫内加量处方，采纳后任务/参数/地图下拉正确联动。
- 迷宫地图重构（2026-08-09，第五轮）：修复旧地图穿墙 bug（s_shape 起点段穿墙、spiral 中段穿墙、ladder 三走廊被通高墙完全封死无可行路线）；重设计为 6 张分级地图（S 形挡板/回形围环·入门，挡板蛇行/五栅长蛇·进阶，四室环路/堡垒要塞·挑战，均含死路支岔提升视觉复杂度）；新增回归测试 tests/unit/test_maze_maps.py（起点→航点折线与所有墙段严格求交 + 越界检查，每张地图≥5 航点）；Telemetry 增 maze_start/maze_goal，迷宫图渲染绿色起点/黄色终点标记与图例。实测：堡垒要塞 12 面墙、起终点标记可见、参考路径 0 越界点、训练 100% 完成，106 测试全过。
- 医患管理系统闭环（2026-08-09，第七轮）：对齐论文医患管理系统框架（医生终端/数据库及管理员终端/患者终端），补齐“计划—派发—执行—回传—评估—优化”闭环：①数据层 TaskAssignment（任务/参数/截止日/状态/完成会话关联）持久化进患者档案；②API GET/POST /api/patients/{id}/assignments，StartRequest 增 assignment_id，训练结束自动销单；③顶栏医生终端/患者终端切换；医生端新增“任务发布”卡（沿用当前训练设置+截止日+派发记录清单）；患者终端：患者登录/任务清单（进入任务）/自主练习（9 任务网格）；④修复潜藏 bug：task_params.task_duration 从未被会话消费（医生端时长滑块与派发时长均失效），start() 改为 duration_s > task_params.task_duration > 配置默认。验证：114 测试全过（新增 4），浏览器实测闭环全通（派发→患者清单→进入任务（派发参数 map=bastion 生效，12 面墙）→完成自动销单→医生端记录变已完成）。框架映射现状：账户及患者管理✔ 任务发布✔ 数据访问✔（档案/历史/趋势） 评估标准✔（Agent 临床评估+处方） 数据库存储✔（患者档案 JSON） 患者任务执行✔；待后续：评估标准制定模块（人群基线配置）、任务日历视图、数据访问大图表。
- 任务体系扩展（2026-08-09，第六轮）：对齐论文运动认知复合任务设计，新增三类任务（共 9 任务）：视觉引导到达（仅显示当前目标逐个到达，参数：目标数量/到达容差/参考速度/时长）、运动拦截（目标沿随机直线走廊往复移动需跟踪拦截，参数：目标移动速度/拦截走廊长度/时长）、目标标记记忆（记忆阶段标记可见→回忆阶段隐藏凭记忆到达，参数：标记轮数/记忆时长/时长），每任务难度均可调。后端：_InterceptPlan/_MarkerMemoryPlan 计划类、目标生成提取为 _random_targets 共用；Telemetry 增 memory_marker；视觉引导模式 task_targets 仅推当前目标。前端：新任务卡/图标/阶段条，目标渲染按任务分流。clinical.py 难度梯度纳入新任务。验证：110 测试全过（新增 4），浏览器实测三任务全部通过（引导模式恰 1 目标环、拦截双端点+移动参考、记忆任务标记显隐切换），detect.mjs CLEAN。
